ind: PersistentVolume
apiVersion: v1
metadata:
  name: postgres-pv2
  namespace: {{ .Values.image.namespace }}
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 100M
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"